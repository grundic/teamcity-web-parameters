<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>teamcity-web-parameters</artifactId>
        <groupId>ru.mail.teamcity</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>build</artifactId>
    <packaging>pom</packaging>
    <properties>
        <maven.build.timestamp.format>yyyyddMMHHmmss</maven.build.timestamp.format>
        <!--<pluginLocation>/Users/grundic/work/Teamcity/PluginDev.Data</pluginLocation>-->
        <pluginLocation>F:\Teamcity\PluginDev.Data</pluginLocation>
    </properties>
    <dependencies>
        <dependency>
            <groupId>ru.mail.teamcity</groupId>
            <artifactId>teamcity-web-parameters-server</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>com.google.code.maven-replacer-plugin</groupId>
                <artifactId>replacer</artifactId>
                <version>1.5.2</version>
                <executions>
                    <execution>
                        <phase>process-sources</phase>
                        <goals>
                            <goal>replace</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <file>${basedir}/../teamcity-plugin.xml</file>
                    <outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
                    <replacements>
                        <replacement>
                            <token>@Version@</token>
                            <value>snapshot-${maven.build.timestamp}</value>
                        </replacement>
                    </replacements>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.4</version>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <finalName>teamcity-web-parameters</finalName>
                            <outputDirectory>${project.parent.build.directory}</outputDirectory>
                            <appendAssemblyId>false</appendAssemblyId>
                            <descriptors>
                                <descriptor>plugin-assembly.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <configuration>
                            <tasks>
                                <copy
                                        file="${project.parent.build.directory}/${parent.artifactId}.zip"
                                        tofile="${pluginLocation}/plugins/${parent.artifactId}.zip"
                                        />
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
